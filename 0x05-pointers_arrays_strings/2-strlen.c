#include "holberton.h"
/**
*strlen -return length of string.
*@s:char type.
*return always 0.
*check the code for holberton school student
int _strlen(char *s)
{
int i;
for(i=0,s[i] != 0;i++;)
return (i);
